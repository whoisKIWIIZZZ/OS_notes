#import "@local/ysz_tools:0.1.0": *
#import "@preview/cuti:0.4.0":*
#show:show-cn-fakebold

#let img(path, caption: "", width: 80%, supplement: [图]) = {
  figure(
    image(path, width: width),
    caption: caption,
    supplement: supplement,
    kind: image,
  )
}
#show: conf.with(
  title: " OS note",
  author: "kiwiizzz & xhkzdepatedream & beigou9",
)
#show "（":"("
#show "）":")"
#show "；": ";"

= Chapter2: 操作系统结构
== 功能概述
操作系统提供的核心功能主要包括以下几个方面：
- *用户接口*：包括命令行接口、批处理接口及图形用户接口。
- *程序执行*：将程序装入内存并运行程序。
- *I/O操作*：操作系统必须提供完成I/O操作的手段。
- *文件系统操纵*：让程序能够读、写、创建和删除文件。
- *通信*：负责进程间交换信息。
- *错误检测*：通过检测错误确保正确运算。
- *资源分配*：把资源分配给同时运行的作业。
- *统计*：记录用户使用资源的类型及数量。
- *保护和安全*：确保所有对资源的访问是受控的且系统不受外界侵犯。

== 操作系统接口
操作系统向用户提供了各种使用其服务功能的手段，即提供了操作系统接口

1. *命令接口*：命令解释程序是用命令接口进行作业控制的主要方式：
  - *脱机控制方式*：用户将对作业的控制要求以作业控制说明书的方式提交给系统，由系统按照规定控制作业的执行。（比如WHU超算的SBATCH）
  - *联机控制方式*：指用户利用系统提供的一组键盘命令或其他操作命令和系统会话，交互式地控制程序的执行。
  键盘命令：
  - 常驻内存的内部命令：当键盘命令功能简单、程序短小、使用频繁。它们在系统初始启动时被引导至内存并常驻内存,这是一个*内部命令*
  - 独立驻留在磁盘上的外部命令：功能复杂的时候,它会*独立作为一个文件*驻留在磁盘上，当需要它们时，再从 磁盘上调入内存运行,也就是*外部命令*.
  - 我们用环境变量来存储命令的路径,例如我们敲击`ls`而不是`/bin/ls`
2. *图形用户接口 (GUI)*：提供基于鼠标的窗口和菜单系统作为接口，通过对出现在屏幕上的对象直接进行操作来控制程序，方便用户使用。


== Syscall & Stack
熟知,系统调用的本质是软中断，使CPU进入内核态;栈和CPU寄存器共同决定了当前程序的执行状态.但是,应用程序和系统拥有*各自的栈*：用户栈、内核栈,我们需要切换栈.栈不同，如何衔接cns

=== x86/x64：半自动机制
在 x86 架构中，CPU 硬件在触发中断时会执行部分自动化操作：
- 通过 TSS (Task State Segment) 找到内核栈位置；
- 自动将用户程序的 `EIP`、`CS`、`EFLAGS` 以及用户态的 `ESP`、`SS` 压入*内核栈*。

*注意：* CPU 虽会自动压栈，但并不自动完成特权级的栈切换，此步骤通常由内核的 Trap Handler 完成。CPU 辅助保存现场，但内核需负责维护栈的平稳切换。


=== RISC-V：全软件控制机制
RISC-V 遵循极简设计原则，硬件不执行任何自动压栈操作，仅通过 `stvec` 寄存器跳转至内核入口。

=== “鸡生蛋”问题与解决方案
由于 CPU 不提供自动压栈，内核必须在切换栈之前保存现场。RISC-V 引入了 `sscratch` 寄存器作为“跳板”，内核预先将内核栈地址存入其中，利用原子指令进行栈切换：

```asm
csrrw sp, sscratch, sp  // 执行后：sp -> 内核栈，sscratch -> 用户栈地址
```
此指令实现栈切换的瞬间不占用任何栈空间，是解决“先有栈还是先存现场”这一死锁问题的精髓。

#table(
columns: (auto, auto, auto),
stroke: 0.5pt,
align: (center, center, center),
[特性], [x86/x64], [RISC-V],
[自动保存寄存器], [✅ 部分自动], [❌ 软件负责],
[自动切换栈], [❌ 软件负责], [❌ 软件负责],
[设计哲学], [CISC，硬件功能丰富], [RISC，硬件极简],
)

#remark[
上述“处理程序”特指内核态的 Trap Handler。当 CPU 跳转至内核入口的瞬间，执行权已完全移交至内核空间，后续的上下文保存与栈管理均由内核代码执行，用户程序对此过程处于完全透明的状态。
]

=== 系统调用与过程调用的区别
- 运行状态不同：
  - 系统调用导致切换到核心态下运行，切换栈
  - 子程序调用不引发任何切换，仍用户态下运行，使用同一个栈，理论上可偷窥调用者栈变量和历史函数的栈遗迹（例如bomblab）
- 进入方式不同
  - 系统调用通过中断机构进入以改变运行状态
  - 子程序直接调用不涉及运行状态改变

== API（Application Programming Interface）
系统调用非常不方便，特定寄存器、特定内存地址，无法用高级语言表达。
API由操作系统商家提供，封装系统调用，形成C语言函数库，目的是方便程序员应用。
API函数运行于用户态。

== 操作系统结构
操作系统是一个大型系统软件，其内核结构主要演变如下：
- *简单结构*：如 MS-DOS 和早期 UNIX。其接口和功能层次没有划分清楚，没有明确定义的结构。
- *层次结构*：划分为若干层（0层为硬件，最高层为用户接口），每层只与相邻层次发生直接联系。其特点是在上下两层不变的基础上可以换掉某层，便于移植和扩充，但以牺牲一定的灵活性为代价。
- *模块结构*：将操作系统内核按照功能划分为一个个独立的模块，只能通过事先规定好的接口方式来调用。其特点是效率高，但全局函数使用多造成访问控制困难，结构不清晰。
- *微内核结构*：
  - *基本思想*：执行与决策分离，缩小内核规模。微内核包括最小的进程和内存管理及通信功能，非基本部分移至系统或用户程序。
  - *优缺点*：降低了开发难度，更安全可靠且容易移植；但增加了大量系统调用和进程切换，总开销增加导致系统性能下降。
  - *现实选择*：大多数主流系统属于宏内核（如 Windows）。鸿蒙系统采用了微内核架构，并通过大量优化解决效率问题。
